"""Utils: shared helpers with no domain logic.

    io      -- read/write JSON/JSONL/YAML, load config, manage run directories
    logging -- consistent logger setup so every script logs the same way
"""
